export interface SliderType {
  name: string,
  imageUrl: string
}

export interface CategoryType {
  id: string,
  name: string,
  icon: string
}

export interface BusinessType {
  about: string,
  address: string,
  category: string,
  contact: string,
  imageUrl: string
  name: string,
  website: string,
}